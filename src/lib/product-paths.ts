/** Canonical URL for an individual product landing/detail page. */
export function productPath(slug: string) {
  return `/${slug}`;
}

/** Canonical URL for a category listing page. */
export function categoryPath(slug: string) {
  return `/${slug}`;
}
