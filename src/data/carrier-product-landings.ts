import type { ProductLanding } from "@/lib/types";

/** Carrier oils moved to CLEAN_PRODUCT_PAGES — kept empty for backwards imports. */
export const CARRIER_PRODUCT_LANDINGS: Record<string, ProductLanding> = {};
